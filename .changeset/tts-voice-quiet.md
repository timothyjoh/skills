---
"timothyjoh-skills": patch
---

`tts-voice` keeps the model's warnings and progress bar out of stderr unless synthesis fails, and is verified to work with no reference WAVs present (`--voice default`).

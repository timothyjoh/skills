---
"timothyjoh-skills": patch
---

`channel-to-skill` gains `--dry-run`: enumerate, triage, and write `scope.json`, then print the estimate and the workflow call without fetching or generating anything.
